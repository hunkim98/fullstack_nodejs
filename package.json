{
  "name": "zerowaste",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && yarn build",
    "dev": "cd server && yarn dev",
    "start": "cd server && yarn start"
  }
}
